{
  "name": "generate-jwt-key",
  "version": "1.0.0",
  "description": "This tool uses an app's private key to generate a jwt token. See [the docs](https://docs.github.com/en/developers/apps/authenticating-with-github-apps#authenticating-as-a-github-app) for full context",
  "main": "generate-jwt-key.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@octokit/auth-app": "^3.4.0",
    "yargs": "^17.0.1"
  }
}
